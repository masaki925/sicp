(require "../common.scm")

; p.159 問3.24
; 表の上の実装では、キーはその等価性を(assoc の呼びだす) equal? を使ってテストする。これは常に適切なテストとは限らない。
; 例えば数値のキーを持つ表があり、探している数値と厳密に一致する必要はなく、ある許容範囲内にあれば良いかもしれない。
; 表の構成子make-table で引数としてキーの等価性テストに使うsame-key? 手続きをとるものを設計せよ。
; make-table は、局所表の適切なlookup やinsert! の手続きへのアクセスに使うdispatch 手続きを返すものとする



